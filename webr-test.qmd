---
title: "WebR test"
format: 
  html:
    code-link: true
engine: knitr
webr: 
  show-startup-message: true    # Disable displaying status of webR initialization
  packages: ['ggplot2', 'dplyr', 'palmerpenguins'] # Install R packages on document open
editor: visual
filters: 
  - webr
---

### install and load some tidyverse packages, palmerpenguins

```{webr-r}


library(dplyr)
library(ggplot2)
library(palmerpenguins)




```

### look at the data set

```{webr-r}

glimpse(penguins)


```

### sample plot

```{webr-r}

penguins |>
  group_by(species) |>
  summarize(avgFlippers=mean(flipper_length_mm, na.rm=TRUE)) |>
  ggplot(mapping=aes(x=species, y=avgFlippers, fill=species)) +
  geom_bar(stat="identity")


```

now your turn! 

```{webr-r}
# create a bar plot comparing average bill length by species



```
